{
  "name": "@chimple/palau-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test --if-present",
    "demo": "pnpm --filter demo dev"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.16",
    "pnpm": "^9.0.0",
    "typescript": "^5.4.0"
  }
}
